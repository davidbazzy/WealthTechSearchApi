# Unit tests mock all infrastructure (no DB, no Docker needed).
# @WebMvcTest slices exclude Flyway/JPA auto-configuration automatically.
spring.jackson.property-naming-strategy=SNAKE_CASE
springdoc.api-docs.enabled=false
api.key=test-key
